<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>{{ site.name }}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="{{ site.name }}">
        <meta name="author" content="Kail">

        <!-- Le styles -->
        <link href="{{ asset('css/bootstrap-flatly.min.css') }}" rel="stylesheet" type="text/css" />
        <link href="{{ asset('fontawesome/css/font-awesome.min.css') }}" rel="stylesheet" type="text/css" />
        <!--<link href="{{ asset('css/hue.css') }}" rel="stylesheet" type="text/css" />-->
        <style type="text/css">
            body {
                padding-top: 80px;
                padding-bottom: 40px;
                background-color: #f5f5f5;
                /*height: 100%; height: 100vh;*/
            }

            .form-signin {
                position: relative;
                max-width: 360px;
                padding: 19px 29px 29px;
                margin: 0 auto 20px;
                background-color: #fff;
                border: 1px solid #e5e5e5;
                -webkit-border-radius: 5px;
                -moz-border-radius: 5px;
                border-radius: 5px;
                -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                box-shadow: 0 1px 2px rgba(0,0,0,.05);
            }
            .form-signin .authoption {
                position: absolute; top: 0; right: 0;
                padding: 0; color: #777;
                -webkit-clip-path: polygon(0 0, 100% 0, 100% 100%);
                clip-path: polygon(0 0, 100% 0, 100% 100%);
                width: 48px; height: 48px; line-height: 48px;
                text-align: right;
                overflow: hidden;
            }
            .form-signin .authoption i {
                position: absolute; top: 0; right: 2px;
                font-size: 54px;
            }

            .form-signin .form-signin-heading,
            .form-signin .checkbox {
                margin-bottom: 10px;
            }
            .form-signin-heading { text-align:center; }
            .form-signin input[type="text"],
            .form-signin input[type="password"] {
                font-size: 16px;
                height: auto;
                margin-bottom: 15px;
                padding: 7px 9px;
            }

        </style>

        <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
        <!--[if lt IE 9]>
          <script src="../assets/js/html5shiv.js"></script>
        <![endif]-->

        <!-- Fav and touch icons -->
        <link rel="apple-touch-icon-precomposed" sizes="144x144" href="../assets/ico/apple-touch-icon-144-precomposed.png">
        <link rel="apple-touch-icon-precomposed" sizes="114x114" href="../assets/ico/apple-touch-icon-114-precomposed.png">
        <link rel="apple-touch-icon-precomposed" sizes="72x72" href="../assets/ico/apple-touch-icon-72-precomposed.png">
        <link rel="apple-touch-icon-precomposed" href="../assets/ico/apple-touch-icon-57-precomposed.png">
        <link rel="shortcut icon" href="{{ asset('images/favico.png') }}">
    </head>

    <body class="comet">

        <div class="container">
            <form class="form-signin" action="{{ path("admin_check") }}" method="POST">
                {#<a class="authoption" href="#">#}
                    {#<i class="fa fa-qrcode"></i>#}
                {#</a>#}

                <h2 class="form-signin-heading">{{ site.name }}-车城</h2>
                {% trans_default_domain 'FOSUserBundle' %}
                
                {% if error is not empty %}
                    <div class="alert alert-danger fade in">
                        <a class="close" data-dismiss="alert">×</a>
                        <strong>{{ error.messageKey|trans(error.messageData, 'security') }}</strong>
                        <!-- <p>你还有4次机会</p> -->
                    </div>
                {% endif %}

                <input type="text" name="_username" class="form-control input-block-level" placeholder="邮箱地址">
                <input type="password" name="_password" class="form-control input-block-level" placeholder="密码">
                <label class="checkbox">
                    <input type="checkbox" name="_remember_me"> 在本设备上记住我
                </label>
                <button class="btn btn-block btn-primary" type="submit">登 入</button>
            </form>

            <div style="margin:80px auto; width:180px; text-align:center;">
                <p>&copy; <a target="_blank" href="{{ site.url }}">{{ site.name }}</a> {{ "now"|date('Y') }}</p>
            </div>
        </div> <!-- /container -->

        <script src="{{ asset('js/jquery-1.9.1.min.js') }}" type="text/javascript"></script>
        <script src="{{ asset('js/bootstrap.min.js') }}" type="text/javascript"></script>

    </body>
</html>
