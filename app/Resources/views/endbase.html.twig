<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}{{ site.name }}{% endblock %}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <link rel="stylesheet" href="{{ asset('fontawesome/css/font-awesome.min.css?v=1') }}" />
        <link rel="stylesheet" href="{{ asset('bootstrap/css/bootstrap.min.css') }}" />

        <style>
            {#html { font-size: 50px; }#}
        </style>
        {% block stylesheets %}{% endblock %}
        <script src="{{ asset('js/jquery-1.9.1.min.js') }}" type="text/javascript"></script>
    </head>
    <body>
{#        {% macro hi(name) %}
            hi {{ name }}
        {% endmacro %}
        
        {% import _self as base %}
        {{ base.hi ('wtf') }}#}
        
        <div id="body">
        {% block body %}
        {% endblock %}
        </div>
        
        <script src="{{ asset('bootstrap/js/bootstrap.min.js') }}" type="text/javascript"></script>
        {#<script src="{{ asset('sweetalert2/sweetalert2.min.js') }}" type="text/javascript"></script>#}
{#        {% if is_inwx() %}
            <script src='http://res.wx.qq.com/open/js/jweixin-1.0.0.js'></script>
            {% include 'wxshare.html.twig' with { settingblock: block('share_setting') } %}
        {% endif %}#}
        <script>
        $(function(){
            // shareSetting.link = window.location.href;
            // var setting = shareSetting.create('timeline');
            // alert(setting.link);
        });
        </script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
